# Base URL of your Express backend (server/server.js). By default it listens
# on the PORT set in server/.env (currently 5002) — set this to
# http://localhost:5002 for local development. It's currently pointed at a
# deployed instance instead:
VITE_API_BASE_URL=https://skillswap2-i72x.onrender.com